return {
    git = require(... .. '.git'),
    lsp = require(... .. '.lsp'),
    misc = require(... .. '.misc'),
    file = require(... .. '.file'),
    mode = require(... .. '.mode'),
    icon = require(... .. '.icon'),
    space = { provider = ' ' },
    spring = { provider = '%=' },
}
